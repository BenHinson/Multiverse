<h1>Cities</h1>
{{#each company}}
    <article>
        <label>
            Name:
            <span>{{this.name}}</span>
        </label>
        <label>
            Population:
            <span>{{this.logoURL}}</span>
        </label>
    </article>
{{/each}}